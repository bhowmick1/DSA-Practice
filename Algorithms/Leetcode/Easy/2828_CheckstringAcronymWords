class Solution {
public:
    bool isAcronym(vector<string>& words, string s) {
        string ans="";
        int n=words.size();
        for(int i=0;i<n;i++){
            ans=ans+words[i][0];
        }
        return ans==s;
    }
};